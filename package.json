{
  "name": "smtp2mqtt",
  "version": "1.0.0",
  "description": "SMTP2MQTT gateway — accept e-mails and pass (some of their) contents to an MQTT broker",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Robert Klep <robert@klep.name>",
  "license": "MIT",
  "dependencies": {
    "jsonpath": "^1.1.1",
    "mailparser": "^3.7.1",
    "mqtt": "^5.10.0",
    "smtp-server": "^3.13.4",
    "yaml": "^2.5.0"
  }
}
